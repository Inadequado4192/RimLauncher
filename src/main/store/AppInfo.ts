import { app } from "electron";
import fs from "fs";
import path from "path";

namespace AppInfo {
    // export const packageJson = JSON.parse(fs.readFileSync(path.join(__dirname, "../../package.json")).toString());
    // export const title = ;
}

export default AppInfo;