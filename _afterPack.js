/** @param {import("electron-builder").AfterPackContext} context */
exports.default = async function (context) {
//     const fs = require("fs");
//     const path = require("path");
//     const appPath = context.appOutDir;

//     const filesToDelete = [
//         // "locales",
//         // "vk_swiftshader.dll", "vk_swiftshader_icd.json", "vulkan-1.dll",
//         // "ffmpeg.dll", // Відео/аудіо декодування (потрібно для <audio> / <video> / стрімінг)
//         // "libEGL.dll", "libGLESv2.dll", // GPU рендеринг (потрібні, якщо не вмикати hardware acceleration)
//         // "d3dcompiler_47.dll", // DirectX шейдери (потрібно для WebGL / Canvas)
//         // "chrome_100_percent.pak", "chrome_200_percent.pak"
//     ];

//     for (const file of filesToDelete) {
//         const filePath = path.join(options.outputPaths[0], file);
//         if (fs.existsSync(filePath)) fs.unlinkSync(filePath);
//     }
}